// A small function to calculate the sum of two numbers using only the variables const and let
const SumAandB =(sum) => (a + b);
let a = 5; // first number
let b = 10; // second number
let sum = SumAandB(); // calculate the sum
console.log(`The sum of ${a} and ${b} is ${sum}`); // output the result
